<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h2 class="card-title"><%= post.title %></h5>
    <p class="card-subtitle mb-2 text-muted">Created by <%= post.author %> on <%= post.createdAt %></h6>
    <p class="card-text"><%= post.description %></p>
  </div>
</div>